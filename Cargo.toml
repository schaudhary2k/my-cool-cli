[package]
name = "my-cool-cli"
version = "0.1.13"  # Bumped version to 0.1.13 for your next release
edition = "2024"
description = "My experimental Rust CLI tool"
authors = ["Saurabh Chaudhary <chaudharysaurabh@gmail.com>"]
homepage = "https://github.com/schaudhary2k/my-cool-cli"
repository = "https://github.com/schaudhary2k/my-cool-cli"
license = "MIT"

[dependencies]

[workspace.metadata.dist]
allow-dirty = ["ci"]

[package.metadata.dist]
# The repository for your Homebrew Formula
tap = "schaudhary2k/homebrew-tap"
# Targets for Mac, Linux, and Windows
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Explicitly tell it which installers to generate
installers = ["shell", "homebrew"]
# NEW: Tell cargo-dist to create .deb files for Ubuntu/Debian users
linux-packages = ["deb"]
# Avoids the MSI installer error on Windows
windows-archive = ".zip"

# NEW: Debian Specific Metadata
[package.metadata.deb]
maintainer = "Saurabh Chaudhary <chaudharysaurabh@gmail.com>"
# This is crucial: it tells the robot WHERE the binary is and WHERE it goes on Linux
assets = [
    ["target/x86_64-unknown-linux-gnu/release/my-cool-cli", "usr/bin/my-cool-cli", "755"],
]

[profile.dist]
inherits = "release"
lto = "thin"
